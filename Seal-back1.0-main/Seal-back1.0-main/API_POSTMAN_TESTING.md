# 📘 เอกสาร API สำหรับทดสอบด้วย Postman — ระบบ SecSeal

> **Base URL:** `http://localhost:3000`
>
> **วันที่จัดทำ:** 11 กุมภาพันธ์ 2569

---

## สารบัญ

1. [การตั้งค่าเบื้องต้น](#1-การตั้งค่าเบื้องต้น)
2. [ระบบยืนยันตัวตน (Auth)](#2-ระบบยืนยันตัวตน-auth)
3. [ผู้ใช้งาน (Users)](#3-ผู้ใช้งาน-users)
4. [ซีล (Seals)](#4-ซีล-seals)
5. [ช่างเทคนิค (Technician)](#5-ช่างเทคนิค-technician)
6. [บันทึกการใช้งาน (Logs)](#6-บันทึกการใช้งาน-logs)
7. [อื่นๆ](#7-อื่นๆ)

---

## 1. การตั้งค่าเบื้องต้น

### Environment Variables (ตั้งค่าใน Postman)

| ตัวแปร | ค่า | คำอธิบาย |
|---|---|---|
| `base_url` | `http://localhost:3000` | URL ของ Server |
| `token` | *(ได้จาก Login API)* | JWT Token สำหรับ User/Admin |
| `tech_token` | *(ได้จาก Technician Login)* | JWT Token สำหรับช่าง |

### การตั้งค่า Headers ที่ใช้บ่อย

**สำหรับ API ที่ต้องยืนยันตัวตน (User/Admin):**
```
Authorization: Bearer {{token}}
Content-Type: application/json
```

**สำหรับ API ของช่างเทคนิค:**
```
Authorization: Bearer {{tech_token}}
Content-Type: application/json
```

---

## 2. ระบบยืนยันตัวตน (Auth)

### 2.1 เข้าสู่ระบบ (Login)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/auth/login` |
| **Auth** | ไม่ต้อง |

**Body (JSON):**
```json
{
  "username": "admin",
  "password": "admin123"
}
```

**บัญชีทดสอบที่ใช้ได้:**

| Username | Password | Role |
|---|---|---|
| `admin` | `admin123` | admin |
| `user` | `user123` | user |

**Response สำเร็จ (200):**
```json
{
  "success": true,
  "message": "Login successful",
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": 1,
    "username": "admin",
    "role": "admin",
    "name": "Administrator",
    "email": "admin@pea.co.th"
  }
}
```

> 💡 **สำคัญ:** คัดลอกค่า `token` ที่ได้ไปตั้งเป็น Environment Variable `{{token}}` ใน Postman

**Response ล้มเหลว (401):**
```json
{
  "error": "Invalid username or password"
}
```

---

## 3. ผู้ใช้งาน (Users)

> ⚠️ ต้องส่ง Header `Authorization: Bearer {{token}}`

### 3.1 ดึงข้อมูลผู้ใช้ตาม Username

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/users/:username` |
| **Auth** | JWT (User/Admin) |

**ตัวอย่าง URL:** `{{base_url}}/api/users/admin`

**Response สำเร็จ (200):**
```json
{
  "id": 1,
  "emp_id": 12345,
  "title_s_desc": "นาย",
  "first_name": "สมชาย",
  "last_name": "ใจดี",
  "username": "admin",
  "email": "admin@pea.co.th",
  "role": "admin",
  "pea_code": "A01",
  "pea_short": "กฟฟ.",
  "pea_name": "การไฟฟ้าส่วนภูมิภาค"
}
```

**Response ล้มเหลว (404):**
```json
{
  "error": "User not found"
}
```

---

### 3.2 สร้างผู้ใช้ใหม่

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/users/` |
| **Auth** | JWT (User/Admin) |

**Body (JSON):**
```json
{
  "emp_id": 99001,
  "title_s_desc": "นาย",
  "first_name": "ทดสอบ",
  "last_name": "ระบบ",
  "username": "testuser01",
  "email": "test01@pea.co.th",
  "role": "user"
}
```

**Response สำเร็จ (201):**
```json
{
  "message": "User created successfully",
  "user": { ... }
}
```

**Response ล้มเหลว (409 — ซ้ำ):**
```json
{
  "error": "Employee ID already exists"
}
```

---

## 4. ซีล (Seals)

### 4.1 ดูซีลทั้งหมด

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/seals/` |
| **Auth** | JWT |

**Response สำเร็จ (200):**
```json
[
  {
    "id": 1,
    "seal_number": "F000000000001",
    "status": "พร้อมใช้งาน",
    "created_at": "2025-03-01T10:00:00Z",
    ...
  }
]
```

---

### 4.2 สร้างซีล (ระบุเลขเริ่มต้น + จำนวน)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/seals/` |
| **Auth** | JWT |

**Body (JSON):**
```json
{
  "seal_number": "F0001",
  "count": 5
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "Seals created successfully",
  "seals": [
    { "seal_number": "F0001", "status": "พร้อมใช้งาน" },
    { "seal_number": "F0002", "status": "พร้อมใช้งาน" },
    { "seal_number": "F0003", "status": "พร้อมใช้งาน" },
    { "seal_number": "F0004", "status": "พร้อมใช้งาน" },
    { "seal_number": "F0005", "status": "พร้อมใช้งาน" }
  ]
}
```

---

### 4.3 สร้างซีลหลาย Batch (Admin เท่านั้น)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/seals/generate` |
| **Auth** | JWT (Admin เท่านั้น) |

**Body (JSON):**
```json
{
  "seal_number": "F2499",
  "count": 3
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "Seals generated successfully",
  "seals": [ ... ]
}
```

**Response ล้มเหลว (403):**
```json
{
  "error": "Access denied, admin only"
}
```

---

### 4.4 สแกนซีล (Barcode)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/seals/scan` |
| **Auth** | JWT |

**Body (JSON):**
```json
{
  "seal_number": "F0001"
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "Seal scanned successfully",
  "seal": {
    "id": 1,
    "seal_number": "F0001",
    "status": "พร้อมใช้งาน",
    ...
  }
}
```

---

### 4.5 รายงานสรุปซีล (Dashboard)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/seals/report` |
| **Auth** | JWT |

**Response สำเร็จ (200):**
```json
{
  "total_seals": 100,
  "พร้อมใช้งาน": 60,
  "จ่าย": 20,
  "ติดตั้งแล้ว": 15,
  "ใช้งานแล้ว": 5
}
```

---

### 4.6 ดูซีลตามสถานะ

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/seals/status/:status` |
| **Auth** | JWT |

**สถานะที่ใช้ได้:**
- `พร้อมใช้งาน`
- `จ่าย`
- `ติดตั้งแล้ว`
- `ใช้งานแล้ว`

**ตัวอย่าง URL:** `{{base_url}}/api/seals/status/%E0%B8%9E%E0%B8%A3%E0%B9%89%E0%B8%AD%E0%B8%A1%E0%B9%83%E0%B8%8A%E0%B9%89%E0%B8%87%E0%B8%B2%E0%B8%99`

> 💡 ใน Postman สามารถพิมพ์ภาษาไทยได้โดยตรง ระบบจะ encode URL ให้อัตโนมัติ

---

### 4.7 ดูซีลตามเลขซีล

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/seals/:seal_number` |
| **Auth** | JWT |

**ตัวอย่าง URL:** `{{base_url}}/api/seals/F0001`

---

### 4.8 จ่ายซีล (Issue)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `PUT` |
| **URL** | `{{base_url}}/api/seals/:seal_number/issue` |
| **Auth** | JWT |
| **Query Params** | `issued_to`, `employee_code`, `remark` |

**ตัวอย่าง URL:**
```
{{base_url}}/api/seals/F0001/issue?issued_to=3&employee_code=12345&remark=จ่ายให้พนักงานตามคำสั่ง
```

**Response สำเร็จ (200):**
```json
{
  "message": "จ่าย Seal เรียบร้อย",
  "seal_number": "F0001",
  "issued_to": 3,
  "employee_code": "12345",
  "remark": "จ่ายให้พนักงานตามคำสั่ง"
}
```

**เงื่อนไข:** ซีลต้องมีสถานะ `"พร้อมใช้งาน"` เท่านั้น

---

### 4.9 ติดตั้งซีล (Use/Install)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `PUT` |
| **URL** | `{{base_url}}/api/seals/:seal_number/use` |
| **Auth** | JWT |

**Body (JSON):**
```json
{
  "serial_number": "MET-2024-001234"
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "ติดตั้ง Seal เรียบร้อย",
  "serial_number": "MET-2024-001234"
}
```

**เงื่อนไข:** ซีลต้องมีสถานะ `"จ่าย"` เท่านั้น

---

### 4.10 คืนซีล / ใช้งานแล้ว (Return)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `PUT` |
| **URL** | `{{base_url}}/api/seals/:seal_number/return` |
| **Auth** | JWT |

**Body (JSON):**
```json
{
  "remarks": "ติดตั้งเรียบร้อย ไม่มีปัญหา"
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "บันทึกเป็น 'ใช้งานแล้ว' เรียบร้อย",
  "remarks": "ติดตั้งเรียบร้อย ไม่มีปัญหา"
}
```

**เงื่อนไข:** ซีลต้องมีสถานะ `"ติดตั้งแล้ว"` เท่านั้น

---

### 4.11 ยกเลิกซีล (Cancel — คืนกลับเป็นพร้อมใช้งาน)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `PUT` |
| **URL** | `{{base_url}}/api/seals/:seal_number/cancel` |
| **Auth** | JWT |

**ตัวอย่าง URL:** `{{base_url}}/api/seals/F0001/cancel`

**Response สำเร็จ (200):**
```json
{
  "message": "ซีล F0001 ถูกคืนสำเร็จ และกลับเป็นสถานะ 'พร้อมใช้งาน'"
}
```

**เงื่อนไข:** ซีลที่มีสถานะ `"ติดตั้งแล้ว"` หรือ `"ใช้งานแล้ว"` ไม่สามารถยกเลิกได้

---

### 4.12 มอบหมายซีลให้ช่าง (Assign by Technician ID)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `PUT` |
| **URL** | `{{base_url}}/api/seals/:seal_number/assign` |
| **Auth** | JWT |

**Body (JSON):**
```json
{
  "technician_id": 1,
  "remark": "มอบหมายให้ช่างติดตั้งมิเตอร์"
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "ซีล F0001 ถูก Assign ให้ช่าง ID 1 เรียบร้อยแล้ว",
  "seal_number": "F0001",
  "technician": 1
}
```

---

### 4.13 มอบหมายซีลหลายอัน (Assign by Technician Code)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/seals/assign-by-techcode` |
| **Auth** | JWT |

**Body (JSON):**
```json
{
  "technician_code": "46735201FNRM-24",
  "seal_numbers": ["F0001", "F0002", "F0003"],
  "remark": "มอบหมายซีลชุดติดตั้งมิเตอร์"
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "Assigned seals successfully",
  "technician_code": "46735201FNRM-24",
  "seals_assigned": ["F0001", "F0002", "F0003"]
}
```

---

### 4.14 จ่ายซีลหลายอันพร้อมกัน (Issue Multiple)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/seals/issue-multiple` |
| **Auth** | JWT |

**Body (JSON):**
```json
{
  "base_seal_number": "F11620000051015",
  "last_numbers": [16, 17, 18],
  "issued_to": 3,
  "employee_code": "12345",
  "remark": "จ่ายให้พนักงานตามคำสั่ง"
}
```

> 💡 ระบบจะสร้างเลขซีลจาก prefix + last_numbers เช่น `F116200000510**16**`, `F116200000510**17**`, `F116200000510**18**`

**Response สำเร็จ (200):**
```json
{
  "message": "Issued multiple seals successfully",
  "seals": [ ... ]
}
```

---

### 4.15 ตรวจสอบซีล (Check)

**แบบที่ 1 — ใช้ Query Params:**

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/seals/check?seal_numbers=F0001,F0002,F9999` |
| **Auth** | JWT |

**Response:**
```json
{
  "unavailable": ["F9999"]
}
```

**แบบที่ 2 — ใช้ JSON Body:**

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/seals/check` |
| **Auth** | JWT |

**Body (JSON):**
```json
{
  "seal_numbers": ["F0001", "F0002", "F9999"]
}
```

**Response:**
```json
{
  "found": ["F0001", "F0002"],
  "unavailable": ["F9999"]
}
```

---

### 4.16 ดู Log ของซีล

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/seals/:seal_number/logs` |
| **Auth** | JWT |

**ตัวอย่าง URL:** `{{base_url}}/api/seals/F0001/logs`

---

## 5. ช่างเทคนิค (Technician)

### 5.1 ลงทะเบียนช่างใหม่

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/technician/register` |
| **Auth** | ไม่ต้อง |

**Body (JSON):**
```json
{
  "technician_code": "46735201FNRM-24",
  "username": "tech_somchai",
  "password": "Pass1234", 
  "first_name": "สมชาย",
  "last_name": "ช่างดี",
  "email": "somchai@company.co.th",
  "phone_number": "081-234-5678",
  "company_name": "บริษัท ช่างดี จำกัด",
  "department": "แผนกติดตั้ง"
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "Technician registered successfully"
}
```

---

### 5.2 เข้าสู่ระบบ (ช่าง)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/technician/login` |
| **Auth** | ไม่ต้อง |

**Body (JSON):**
```json
{
  "username": "tech_somchai",
  "password": "Pass1234"
}
```

**Response สำเร็จ (200):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIs..."
}
```

> 💡 คัดลอกค่า `token` ไปตั้งเป็น Environment Variable `{{tech_token}}`

---

### 5.3 นำเข้ารายชื่อช่าง (Bulk Import)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/technician/import` |
| **Auth** | ไม่ต้อง |

**Body (JSON — Array):**
```json
[
  {
    "technician_code": "TECH-001",
    "username": "tech01",
    "password": "default123",
    "first_name": "ช่าง",
    "last_name": "หนึ่ง",
    "email": "tech01@company.co.th",
    "phone_number": "081-111-1111"
  },
  {
    "technician_code": "TECH-002",
    "username": "tech02",
    "first_name": "ช่าง",
    "last_name": "สอง",
    "email": "tech02@company.co.th",
    "phone_number": "081-222-2222"
  }
]
```

> 💡 ถ้าไม่ระบุ `password` ระบบจะใช้ค่าเริ่มต้น `"default123"`

**Response สำเร็จ (200):**
```json
{
  "message": "Imported successfully",
  "count": 2
}
```

---

### 5.4 ดูรายชื่อช่างทั้งหมด

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/technician/list` |
| **Auth** | ไม่ต้อง |

---

### 5.5 อัปเดตข้อมูลช่าง

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `PUT` |
| **URL** | `{{base_url}}/api/technician/update/:id` |
| **Auth** | ไม่ต้อง |

**ตัวอย่าง URL:** `{{base_url}}/api/technician/update/1`

**Body (JSON):**
```json
{
  "first_name": "สมชาย",
  "last_name": "ช่างเก่ง",
  "phone_number": "089-999-8888",
  "company_name": "บริษัท ช่างเก่ง จำกัด",
  "department": "แผนกซ่อมบำรุง"
}
```

---

### 5.6 ลบช่าง

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `DELETE` |
| **URL** | `{{base_url}}/api/technician/delete/:id` |
| **Auth** | ไม่ต้อง |

**ตัวอย่าง URL:** `{{base_url}}/api/technician/delete/1`

---

### 5.7 ดูซีลทั้งหมดที่จ่ายให้ช่าง (สำหรับ User/Admin)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/technician/seals` |
| **Auth** | JWT (User/Admin) |

---

### 5.8 ดูซีลที่ได้รับมอบหมาย (เฉพาะช่างเจ้าตัว)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/technician/my-seals` |
| **Auth** | JWT (ช่าง — `{{tech_token}}`) |

---

### 5.9 ช่างติดตั้งซีล

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `PUT` |
| **URL** | `{{base_url}}/api/technician/seals/install` |
| **Auth** | JWT (ช่าง — `{{tech_token}}`) |

**Body (JSON):**
```json
{
  "seal_number": "F0001",
  "serial_number": "MET-2024-001234"
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "ติดตั้งซีลสำเร็จ กรุณาอัปโหลดรูปภาพ",
  "seal_number": "F0001",
  "serial_number": "MET-2024-001234"
}
```

**เงื่อนไข:**
- ซีลต้องถูก Assign ให้ช่างคนนี้
- ซีลต้องมีสถานะ `"จ่าย"`

---

### 5.10 ช่างคืนซีล

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `PUT` |
| **URL** | `{{base_url}}/api/technician/seals/return/:seal_number` |
| **Auth** | JWT (ช่าง — `{{tech_token}}`) |

**ตัวอย่าง URL:** `{{base_url}}/api/technician/seals/return/F0001`

**Body (JSON):**
```json
{
  "remarks": "ติดตั้งเรียบร้อย ตรวจสอบแล้ว"
}
```

**Response สำเร็จ (200):**
```json
{
  "message": "คืน Seal สำเร็จ",
  "seal_number": "F0001",
  "remarks": "ติดตั้งเรียบร้อย ตรวจสอบแล้ว"
}
```

---

### 5.11 ช่างอัปโหลดรูปซีล

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/technician/seals/upload-images` |
| **Auth** | JWT (ช่าง — `{{tech_token}}`) |
| **Content-Type** | `multipart/form-data` |  

**Body (form-data):**

| Key | Type | Value |
|---|---|---|
| `seal_number` | Text | `F0001` |
| `image1` | File | *(เลือกไฟล์รูปภาพ)* |
| `image2` | File | *(เลือกไฟล์รูปภาพ)* |

> ⚠️ ใน Postman ต้องเลือก Body type เป็น **form-data** (ไม่ใช่ raw JSON)

**Response สำเร็จ (200):**
```json
{
  "message": "อัปโหลดรูปสำเร็จ",
  "seal_number": "F0001",
  "image1": "/uploads/1234567890_photo1.jpg",
  "image2": "/uploads/1234567890_photo2.jpg"
}
```

**เงื่อนไข:**
- ซีลต้องมีสถานะ `"ติดตั้งแล้ว"`
- ช่างต้องเป็นคนที่ติดตั้งซีลนั้น

---

## 6. บันทึกการใช้งาน (Logs)

> ⚠️ ต้องส่ง Header `Authorization: Bearer {{token}}` (ต้องเป็น Admin สำหรับบาง API)

### 6.1 สร้าง Log ใหม่

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `POST` |
| **URL** | `{{base_url}}/api/logs/` |
| **Auth** | JWT |

**Body (JSON):**
```json
{
  "user_id": 1,
  "action": "ทดสอบสร้าง log"
}
```

---

### 6.2 ดู Log ทั้งหมด (แยกตามประเภท)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/logs/` |
| **Auth** | JWT |

**Response สำเร็จ (200):**
```json
{
  "success": true,
  "logs": {
    "created": [ ... ],
    "issued": [ ... ],
    "used": [ ... ],
    "returned": [ ... ],
    "other": [ ... ]
  }
}
```

---

### 6.3 ดู Log แยกตามประเภทเฉพาะ

| Method | URL | คำอธิบาย |
|---|---|---|
| `GET` | `{{base_url}}/api/logs/created` | Log การสร้างซีล |
| `GET` | `{{base_url}}/api/logs/issued` | Log การจ่ายซีล |
| `GET` | `{{base_url}}/api/logs/used` | Log การใช้งานซีล |
| `GET` | `{{base_url}}/api/logs/returned` | Log การคืนซีล |

---

### 6.4 ดู Log ตาม ID

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/logs/:log_id` |
| **Auth** | JWT |

---

### 6.5 ดู Log ตามประเภท (Type)

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/logs/type/:log_type` |
| **Auth** | JWT |

**ตัวอย่าง URL:** `{{base_url}}/api/logs/type/จ่ายซิล`

---

### 6.6 ดู Log ตามผู้ใช้งาน

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/logs/user/:user_id` |
| **Auth** | JWT |

**ตัวอย่าง URL:** `{{base_url}}/api/logs/user/1`

---

### 6.7 ดู Log ตามช่วงเวลา

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/logs/range?start=2025-03-01&end=2025-03-31` |
| **Auth** | JWT |

---

### 6.8 ลบ Log

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `DELETE` |
| **URL** | `{{base_url}}/api/logs/:log_id` |
| **Auth** | JWT |

---

## 7. อื่นๆ

### 7.1 Health Check

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/api/health` |
| **Auth** | ไม่ต้อง |

**Response (200):**
```json
{
  "status": "ok",
  "time": "2025-03-01T12:00:00Z"
}
```

### 7.2 ดูรูปที่อัปโหลด

| หัวข้อ | รายละเอียด |
|---|---|
| **Method** | `GET` |
| **URL** | `{{base_url}}/uploads/:filename` |
| **Auth** | ไม่ต้อง |

---

## 📋 ลำดับการทดสอบแนะนำ (Testing Flow)

### Flow 1: Admin สร้างและจ่ายซีล
```
1. POST /api/auth/login              → ล็อกอินเป็น admin
2. POST /api/seals/                   → สร้างซีล 5 อัน
3. GET  /api/seals/report             → ดูรายงานสรุป
4. POST /api/technician/register      → ลงทะเบียนช่าง
5. POST /api/seals/assign-by-techcode → มอบหมายซีลให้ช่าง
6. GET  /api/seals/status/จ่าย        → ตรวจสอบว่าซีลเปลี่ยนสถานะแล้ว
```

### Flow 2: ช่างติดตั้งและคืนซีล
```
1. POST /api/technician/login                  → ล็อกอินเป็นช่าง
2. GET  /api/technician/my-seals               → ดูซีลที่ได้รับมอบหมาย
3. PUT  /api/technician/seals/install          → ติดตั้งซีล
4. POST /api/technician/seals/upload-images    → อัปโหลดรูปหลังติดตั้ง
5. PUT  /api/technician/seals/return/:seal     → คืนซีล
```

### Flow 3: Admin ดู Log และรายงาน
```
1. POST /api/auth/login    → ล็อกอินเป็น admin
2. GET  /api/logs/          → ดู Log ทั้งหมด
3. GET  /api/seals/report   → ดูรายงานสรุป
4. GET  /api/seals/F0001/logs → ดู Log เฉพาะซีล
```
